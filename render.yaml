services:
  - type: web
    name: my-web-service
    buildCommand: npm install && npm run build # Installs dependencies and builds the project
    routes:
      - type: rewrite
        source: /.*
        destination: /